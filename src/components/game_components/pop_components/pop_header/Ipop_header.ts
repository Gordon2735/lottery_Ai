'use strict';

// Pop Header Component Interfaces

interface IpopHeaderSharedHTML {
    shell: string;
    container: string;
    results: string;
}
interface IpopHeaderSharedStyles {
    root: string;
    shell: string;
    container: string;
    results: string;
}

export type { IpopHeaderSharedHTML, IpopHeaderSharedStyles };
