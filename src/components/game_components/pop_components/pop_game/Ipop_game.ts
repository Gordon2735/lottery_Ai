'use strict';

// Pop Component Interfaces

interface IpopGameSharedHTML {
    shell: string;
    container: string;
    results: string;
}
interface IpopGameSharedStyles {
    root: string;
    shell: string;
    container: string;
    results: string;
}

export type { IpopGameSharedHTML, IpopGameSharedStyles };
